# PaPaPhone

Голосовой телефон для людей с потерей зрения. Полностью офлайн, управление только голосом.

Работает на Orange Pi Zero 2W + SIM7600G-H 4G HAT. Можно адаптировать под любой Linux.

## Как это работает

Дедушка говорит **"телефон позвони маме"** -- одной фразой. Система распознаёт речь, находит контакт по имени (с учётом склонений: "маме" = "мама"), переспрашивает подтверждение и звонит. Всё офлайн, без интернета.

Архитектура -- конечный автомат (FSM) с пятью состояниями. Входящие звонки и SMS обнаруживаются автоматически фоновыми потоками.

## Голосовые команды

После wake-фразы **"телефон"** (или в одной фразе с ней):

### Звонки

| Команда | Пример | Что происходит |
|---------|--------|----------------|
| Позвонить контакту | "позвони маме", "набери папе" | Поиск по имени (морфология) -> подтверждение -> звонок |
| Набрать номер | "набери номер 89001234567" | Подтверждение -> набор |
| Найти контакт | "найди контакт маму" | Поиск -> "Позвонить? Скажите да." |
| Ответить | "ответь", "принять звонок" | Принять входящий |
| Положить трубку | "положи трубку", "отбой" | Завершить звонок |

### SMS

| Команда | Пример | Что происходит |
|---------|--------|----------------|
| Последние SMS | "последние смс", "прочитай смс" | Все SMS по одному: дальше / позвонить / хватит |
| Непрочитанные | "непрочитанные смс", "новые смс" | Только непрочитанные |
| *(автоматически)* | | Новые SMS озвучиваются при поступлении. Ночью копятся, утром зачитываются |

### Информация

| Команда | Пример |
|---------|--------|
| Время | "который час", "сколько время" |
| Список функций | "команды", "что ты умеешь" |
| Адрес | "адрес", "мой адрес", "где я живу" |

### Экстренные

| Команда | Пример | Что происходит |
|---------|--------|----------------|
| Экстренный вызов | "нужна помощь", "скорая", "спасите", "мне плохо" | Звонок 112. Тишина = звонить (не отменять) |

### Контакты

| Команда | Пример |
|---------|--------|
| Добавить контакт | "добавь контакт", "новый контакт" |

Номер диктуется по цифрам или группами ("восемь девять ноль ноль"). Поддерживаются числительные. Автостоп на 11 цифрах.

### Управление

| Команда | Пример | Что происходит |
|---------|--------|----------------|
| Отмена | "стоп", "отмена", "отменить" | Прервать текущее действие (с подтверждением) |

### Секретное меню

Длинные фразы, которые дедушка случайно не скажет:

| Команда | Пример |
|---------|--------|
| Секретное меню | "секретное меню" -- вход, далее: имя -> удалить / экстренный / убрать из экстренных |

## Входящие звонки

Система обнаруживает входящий через AT+CLCC каждые 2 секунды. Определяет кто звонит по контактам. Спрашивает "Входящий от Мама. Ответить?" Молчание = сброс (безопасный дефолт).

## Конечный автомат (FSM)

```
IDLE -> LISTENING -> EXECUTING -> IDLE
  |        |            |
  |        |            +-> IN_CALL -> IDLE
  |        |
  +-> INCOMING -> IDLE
```

- **IDLE** -- ждёт wake-фразу, проверяет входящие звонки и SMS
- **LISTENING** -- wake получен, ждёт команду (12 с)
- **EXECUTING** -- сценарий выполняется
- **IN_CALL** -- активный звонок, слушает "положи трубку"
- **INCOMING** -- обработка входящего звонка

Команда может быть в одной фразе с wake: "телефон позвони маме" -- без паузы.

## Технологии

| Компонент | Технология |
|-----------|------------|
| Распознавание речи | [Vosk](https://alphacephei.com/vosk/) (офлайн) |
| Синтез речи | [Piper TTS](https://github.com/rhasspy/piper) (офлайн, нейросеть) |
| Модем | SIM7600G-H по AT-командам (pyserial) |
| Контакты | SQLite + pymorphy3 (морфология) |
| Команды | YAML-словарь + резолв имён через БД |

## Оборудование

- Orange Pi Zero 2W (4 GB RAM) или любой Linux ARM/x86
- SIM7600G-H 4G HAT (UART/USB)
- USB-микрофон
- Динамик (USB-аудио или 3.5 мм)

## Установка

```bash
bash deploy.sh
```

Скрипт установит зависимости, скачает модели Vosk (1.8 GB) и Piper, создаст systemd-сервис.

### Ручная установка

```bash
sudo apt install python3 python3-venv portaudio19-dev
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# Модель Vosk (русская, 45 MB)
mkdir -p models
wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
unzip vosk-model-small-ru-0.22.zip -d models/

# Модель Piper TTS
BASE=https://huggingface.co/rhasspy/piper-voices/resolve/main/ru/ru_RU/ruslan/medium
wget -P models/ $BASE/ru_RU-ruslan-medium.onnx
wget -P models/ $BASE/ru_RU-ruslan-medium.onnx.json

# Конфигурация
cp data/.env.example data/.env
nano data/.env
```

## Запуск

```bash
# Продакшн (Orange Pi + модем)
python -m src.main

# Отладка на Windows/Linux без модема
python -m src.main --demo

# Без wake-фразы (быстрее тестировать)
python -m src.main --demo --no-wake
```

### Управление сервисом (после deploy.sh)

```bash
sudo systemctl start papaphone       # запустить
sudo systemctl stop papaphone        # остановить
sudo systemctl restart papaphone     # перезагрузить
sudo systemctl status papaphone      # статус (работает / упал / код ошибки)
journalctl -u papaphone -f           # логи в реальном времени
journalctl -u papaphone --since today # логи за сегодня
```

Сервис запускается автоматически при включении устройства. При ошибке перезапускается через 10 секунд.

## Конфигурация

Файл `data/.env` (создаётся при `deploy.sh` или вручную из `data/.env.example`):

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `PAPAPHONE_VOSK_MODEL` | `vosk-model-small-ru-0.22` | Папка модели в `models/` |
| `PAPAPHONE_MODEM_PORT` | `/dev/ttyUSB0` | Порт SIM7600 |
| `PAPAPHONE_WAKE_PHRASES` | `телефон,папафон` | Wake-фразы через запятую |
| `PAPAPHONE_EMERGENCY_NUMBER` | `112` | Номер экстренного вызова |
| `PAPAPHONE_HOME_ADDRESS` | *(не задан)* | Домашний адрес (команда "адрес") |
| `PAPAPHONE_EMERGENCY_CONTACTS` | *(не задан)* | Формат: `Сын:+79001234567,Дочь:+79009876543` |
| `PAPAPHONE_AUDIO_INPUT_DEVICE` | *(системный)* | Индекс ALSA микрофона |
| `PAPAPHONE_AUDIO_OUTPUT_DEVICE` | *(системный)* | Индекс ALSA динамика |
| `PAPAPHONE_PIPER_MODEL` | `models/ru_RU-ruslan-medium.onnx` | Путь к модели Piper |

## Структура проекта

```
PaPaPhone/
├── src/
│   ├── main.py              # Точка входа, инициализация
│   ├── config.py            # Конфигурация через env
│   ├── fsm/
│   │   ├── engine.py        # Конечный автомат (PhoneFSM)
│   │   ├── states.py        # Enum состояний
│   │   ├── incoming_monitor.py  # Фоновый поток входящих звонков
│   │   └── sms_monitor.py   # Фоновый поток входящих SMS
│   ├── voice/
│   │   ├── asr.py           # Vosk: распознавание речи
│   │   └── tts.py           # Piper: синтез речи
│   ├── modem/
│   │   ├── serial_io.py     # UART / AT-команды
│   │   ├── at_commands.py   # Инициализация, сигнал
│   │   ├── call.py          # Звонки, определитель номера
│   │   └── sms.py           # Чтение/отправка SMS
│   ├── contacts/
│   │   ├── db.py            # SQLite + морфологический поиск
│   │   └── schema.sql
│   ├── commands/
│   │   ├── dictionary.py    # Загрузка YAML-словаря
│   │   └── executor.py      # Сопоставление текста с командой
│   └── scenarios/
│       ├── base.py          # ScenarioContext, CancelledError, _confirm()
│       ├── call.py          # Звонки, экстренный вызов, ожидание конца
│       ├── info.py          # Время, команды, адрес
│       ├── sms.py           # Чтение SMS с пагинацией и звонком
│       ├── contacts.py      # Добавление, поиск, секретное меню
│       └── __init__.py      # get_scenario() -- роутер
├── data/
│   ├── commands.yaml        # Словарь голосовых команд
│   └── test_scenarios.md    # Тест-план
├── models/                  # Vosk + Piper (.gitignore)
├── requirements.txt
├── deploy.sh
├── LICENSE
└── README.md
```

## Автор

**Igor Kriusov** -- [kriusovia@gmail.com](mailto:kriusovia@gmail.com)
GitHub: [https://github.com/Volk-Arch/PaPaPhone](https://github.com/Volk-Arch/PaPaPhone)

## Лицензия

**[PolyForm Noncommercial License 1.0.0](https://polyformproject.org/licenses/noncommercial/1.0.0/)** -- бесплатно для личного и некоммерческого использования.

Зависимости: Vosk (Apache 2.0), Piper TTS (MIT), pymorphy3 (MIT).
